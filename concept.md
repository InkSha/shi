# 诗语言：一种面向人类表达的可执行文档语言设想

## 一、概述

传统编程语言要求人类将思维转换为严格的代码结构：

```
人类想法
    ↓
程序设计
    ↓
代码
    ↓
机器执行
```

这种方式要求开发者掌握固定语法、抽象结构和编程范式。

随着 AI 能理解自然语言，以及软件系统越来越偏向复杂业务逻辑，一个新的问题出现：

> 是否可以让人类直接使用接近自然语言的方式描述世界、规则和行为，而由系统将其转换为可执行程序？

“诗语言”的设想正是在探索这一方向：

> 以文章作为程序主体，以术语作为概念基础，以标记语句作为执行逻辑。

它不是简单地将英文关键字翻译成中文，而是尝试让“人类描述事物的方式”成为程序的第一表达形式。

---

# 二、核心理念

## 1. 文件不是代码，而是一篇文章

传统项目：

```
User.ts
Order.ts
Payment.ts
```

关注：

* 类
* 方法
* 数据结构

诗语言：

```
《用户系统》
《订单规则》
《支付流程》
```

关注：

* 概念
* 关系
* 规则
* 行为

一个文件更像一篇领域文章。

例如：

```
《图书系统》

图书是一种记录知识的载体。

图书拥有名称、作者和库存。

借阅图书后，库存会减少。
```

这些内容首先是知识描述，而不是代码。

---

# 三、代码与文章的关系

核心设计：

> 默认所有文本都是描述内容，只有被明确标记的部分才是程序。

例如：

普通文本：

```
用户登录后进入首页。
```

表示：

* 产品需求
* 业务描述
* 文档说明

不会直接执行。

执行文本：

```
「用户登录成功后，进入首页」
```

表示：

* 创建行为
* 生成程序逻辑

这种设计反转传统关系：

传统：

```
代码 = 主体
注释 = 辅助
```

诗语言：

```
文章 = 主体
代码 = 被标记的行为
```

---

# 四、术语系统

自然语言最大的问题不是表达能力不足，而是含义不稳定。

例如：

“删除用户”可能表示：

* 永久删除数据
* 禁用账号
* 隐藏用户
* 移除权限

因此诗语言引入项目级术语定义。

例如：

```
# 术语定义

用户：

完成注册并拥有身份的人。


订单：

用户购买行为产生的交易记录。


删除：

永久移除数据，不保留恢复能力。
```

之后：

```
「删除用户」
```

系统理解为：

```
DeleteUser
```

而不是依靠猜测。

---

# 五、自然表达与语义统一

诗语言不希望强制固定关键词。

传统：

```
if
for
class
```

只能使用固定写法。

诗语言允许多个自然表达：

```
如果余额不足

假如余额不足

倘若余额不足

当余额不足时
```

这些表达最终归属于同一个语义：

```
条件判断
```

结构：

```
自然表达

    ↓

语义概念

    ↓

程序结构
```

重点不是规定人如何说话，而是让系统理解表达背后的概念。

---

# 六、文章结构与程序结构

文章结构可以映射程序结构。

## 标题

对应：

模块。

例如：

```
《用户系统》
```

对应：

```
UserModule
```

## 定义

对应：

类型系统。

例如：

```
图书：

拥有：
名称
作者
库存
```

对应：

```
Type Book
```

## 引用

对应：

模块依赖。

例如：

```
参考《支付系统》
```

对应：

```
Import PaymentSystem
```

## 行为

对应：

函数或事件。

例如：

```
「用户购买商品」
```

对应：

```
Purchase()
```

---

# 七、示例

一个简单的电商系统：

```
《商品购买》

商品是一种可以被购买的物品。

商品拥有名称、价格和库存。


用户拥有账户余额。


购买商品：

「检查商品库存」

「检查用户余额」

「创建订单」

「减少商品库存」

「扣除用户余额」
```

系统可以解析为：

```
商品
    |
    +-- 名称
    +-- 价格
    +-- 库存


购买流程

    检查库存
        ↓
    检查余额
        ↓
    创建订单
        ↓
    修改状态
```

最终生成传统程序。

---

# 八、与现有技术的关系

## 1. 与中文编程的区别

传统中文编程：

```
英文语法
    ↓
中文替换
```

例如：

```
if → 如果
class → 类
```

本质仍然是传统代码。

诗语言：

```
自然语言
    ↓
语义模型
    ↓
程序
```

关注的是表达方式和知识结构。

---

## 2. 与自然语言编程的区别

当前 AI 编程：

```
需求描述
    ↓
AI生成代码
```

代码仍然是最终产物。

诗语言：

```
文章本身
    ↓
语义模型
    ↓
程序
```

文章长期存在，是系统的一部分。

---

## 3. 与 DSL 的关系

领域语言：

```
业务语言
    ↓
程序
```

诗语言进一步扩展：

```
文章
    ↓
领域知识
    ↓
程序
```

---

## 4. 与 Literate Programming 的关系

文学化编程：

```
代码 + 文档
```

诗语言：

```
文档本身就是程序
```

---

# 九、可能的发展方向

如果成熟，它可能适合：

## AI Agent

Agent 的核心：

* 角色定义
* 世界规则
* 行为约束
* 任务流程

天然适合文章化描述。

## 游戏世界

例如：

```
世界规则：

夜晚会出现怪物。

玩家击败怪物后获得经验。
```

这些本身就是世界设定。

## 企业业务系统

复杂业务通常已经存在大量：

* 产品文档
* 流程文档
* 规则说明

诗语言可以尝试让这些文档直接参与运行。

---

# 十、主要挑战

## 1. 语义歧义

自然语言自由度越高，解析难度越高。

需要：

* 术语定义
* 上下文
* 版本管理

## 2. 项目协作

如果所有人都自由表达：

```
创建用户

新增用户

生成账号
```

可能产生混乱。

因此需要：

自然表达自由 + 内部语义统一。

## 3. 调试方式

传统：

```
第20行报错
```

未来可能：

```
《订单流程》

“扣除余额”

无法找到余额定义。
```

需要新的调试模型。

---

# 十一、最终定位

诗语言不是：

“中文替代 Java”。

它更像：

> 一种面向人类知识表达的可执行文档语言。

目标不是让程序员写更像中文的代码，而是让：

```
人类描述世界
        ↓
形成结构化知识
        ↓
自动生成软件
```

未来的软件可能不再首先由代码构成，而是由：

* 世界描述
* 领域定义
* 规则体系
* 行为关系

组成。

代码只是这些知识经过编译后的结果。
